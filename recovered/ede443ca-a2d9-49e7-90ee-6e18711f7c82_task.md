# Book Overview Redesign Tasks

- [x] Create new page `src/app/(main)/book/[id]/page.tsx`.
- [x] Implement the split layout (Beige `#F1EEE3` top, White `#FDFBF7` bottom) on the new page.
- [x] Recreate the layout from the image (floating book cover, title/author on right, description and metadata below).
- [x] Update `src/app/(main)/page.tsx` to push to `/book/[id]` on card click instead of opening modal.
- [x] Update `src/app/(main)/library/page.tsx` to push to `/book/[id]` on card click (No change needed, library has its own logic).
- [x] Update `src/app/(main)/bookmarks/page.tsx` to push to `/book/[id]` on card click.
- [x] Remove `OverviewModal.tsx` entirely.
- [x] Test navigation and layout.